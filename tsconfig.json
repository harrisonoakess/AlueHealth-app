// tsconfig.json
{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "baseUrl": ".",                // 👈 add this
    "jsx": "react-jsx",
    "jsxImportSource": "nativewind",
    "strict": true,
    "paths": {
      "@/*": ["app/*"]             // 👈 maps "@/..." to "app/..."
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "nativewind-env.d.ts"
  ]
}
