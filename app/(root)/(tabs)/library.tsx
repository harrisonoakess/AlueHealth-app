import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';


const Library = () => {
  return (
    <SafeAreaView>
      <Text>Welcome to the Library Screen!</Text>
    </SafeAreaView>
  );
}

export default Library;